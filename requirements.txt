# Runtime dependencies.
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1

# Optional: powers the free-form assistant panel. Without it (or without an
# API key) the panel falls back to the deterministic local metrics engine.
anthropic>=1.3.0
